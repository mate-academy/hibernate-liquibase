databaseChangeLog:
  - changeSet:
      id: 1
      author: your_name
      comment: Create Actor table
      createTable:
        tableName: actors
        columns:
          - column:
              name: id
              type: bigint
              autoIncrement: true
              constraints:
                primaryKey: true
          - column:
              name: name
              type: varchar(255)
          # Add more columns as needed
