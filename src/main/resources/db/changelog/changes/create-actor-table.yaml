databaseChangeLog:
  - changeSet:
      id: create-actor-table
      author: admin
      changes:
        - createTable:
            tableName: actors
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: varchar(255)
              - column:
                  name: country_id
                  type: bigint
